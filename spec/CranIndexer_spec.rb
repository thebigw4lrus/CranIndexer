require 'spec_helper'

describe CranIndexer do
  it 'has a version number' do
    expect(CranIndexer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
